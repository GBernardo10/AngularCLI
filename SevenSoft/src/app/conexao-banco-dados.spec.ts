import { ConexaoBancoDados } from './conexao-banco-dados';

describe('ConexaoBancoDados', () => {
  it('should create an instance', () => {
    expect(new ConexaoBancoDados()).toBeTruthy();
  });
});
